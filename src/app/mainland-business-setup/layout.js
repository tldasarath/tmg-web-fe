import { buildMetadata } from "@/lib/seo";

export const metadata = buildMetadata({
  title: "Mainland Business Setup Dubai | TMG Global",
  description:
    "Set up your Mainland company in Dubai with TMG Global: licensing, visas, and ongoing compliance.",
  path: "/mainland-business-setup",
});

export default function MainlandLayout({ children }) {
  return children;
}


