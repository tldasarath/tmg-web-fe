import { buildMetadata } from "@/lib/seo";

export const metadata = buildMetadata({
  title: "Freezone Business Setup UAE | TMG Global",
  description:
    "Launch a Freezone company in the UAE with 100% ownership. TMG Global handles applications, licensing, and visas.",
  path: "/freezone-company-formation-dubai",
});

export default function FreezoneLayout({ children }) {
  return children;
}


